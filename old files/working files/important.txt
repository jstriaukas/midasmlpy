to build run 
python3 setup_for_platform.py #it's detect your OS and run setup for that

On windows you have Visual studio 2013 or higher
Important:
there is a file armadillo_bits.rar extract first it inside include folder
If you unable to compile for Mac
then 
brew install lapack
brew install openblas
then rebuild
Only use python3 version
If not builded then use 
python3 mac/setup.py
